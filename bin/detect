#!/usr/bin/env ruby
puts "Checking #{ARGV.inspect}"
# this pack is valid for apps with an odbc.ini in the root
if File.exists?(ARGV[0])
  puts "ODBC INI file #{ARGV[0]} detected."
  exit 0
else
  exit 1
end
