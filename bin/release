#!/usr/bin/env ruby

DIR = "#{ARGV[0]}/vendor/unixodbc"

puts <<-CONF
---
config_vars:
  PATH: "#{ENV['PATH'].gsub('::',':')}:#{DIR}/bin"
  ODBC-DIR: "#{DIR}"
  LD_LIBRARY_PATH: "#{DIR}/lib"
  LIBRARY_PATH: "#{DIR}/lib"
  CPPATH: "#{DIR}/include"
  CPATH: "#{DIR}/include"
CONF
